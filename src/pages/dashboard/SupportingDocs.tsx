import AnimatedSection from "@/components/AnimatedSection";

const SupportingDocs = () => (
  <AnimatedSection>
    <h1 className="font-display text-3xl font-bold mb-4">Supporting Documents</h1>
    <p className="text-muted-foreground">Upload and manage your supporting documents here.</p>
  </AnimatedSection>
);

export default SupportingDocs;
